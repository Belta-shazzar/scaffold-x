describe("Auth service unit test", () => {
    beforeEach(() => {
      jest.clearAllMocks();
    });
  describe("Sign up", () => {
    it("should successfully register a new user", async () => {});
    it("should throw errors if the user already exist", async () => {});
  });
  describe("Sign in", () => {
    it("should successfully log user in", async () => {});
    it("should throw error for invalid credentials", async () => {});
  });

  describe("utility function", () => {
    it("should create authentication token", () => {});
  });
});
